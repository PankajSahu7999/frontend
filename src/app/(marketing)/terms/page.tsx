import TermsAndServicesPage, { metadata as termsMeta } from "../terms-and-services/page";

export const metadata = termsMeta;

export default function TermsPage() {
  return <TermsAndServicesPage />;
}
